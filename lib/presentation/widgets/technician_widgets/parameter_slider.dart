import 'package:flutter/material.dart';

class ParameterSettings extends StatelessWidget {
  const ParameterSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const ExpansionTile(
      title: Text('Paramètres Tension'),
      children: [
        // Add sliders from EcranParametrageOnduleur snippet
      ],
    );
  }
}
