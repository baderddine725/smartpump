import 'package:flutter/material.dart';

class MultiSiteMap extends StatelessWidget {
  const MultiSiteMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text('Carte des sites'), // Add map integration if needed
    );
  }
}
